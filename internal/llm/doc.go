// Package llm defines a provider-agnostic Client interface for language-model
// calls, with an anthropic/ implementation and version-controlled prompts/.
// Handlers and services depend on the interface, never on the SDK directly.
package llm
